# Codex Instructions For Recall Radar

Work only inside this repo:

`C:\Users\pc\codex-projects\recall-radar-web`

Project-specific rules:

- Do not touch, inspect, modify, read, list, or reuse files from other projects.
- Do not connect or create remote services.
- Do not use Docker.
- Do not create real `.env`, `.env.local`, `.env.production`, or secret files.
- `.env.example` is allowed.
- Do not modify global npm config, global git config, PATH, or Windows environment variables.
- Do not deploy.
- Do not run Cloudflare, Wrangler, GitHub CLI, Render, Vercel, Netlify, or similar remote-service commands.
- Do not add Git remotes.
- Do not commit unless explicitly asked.
- Use local port `5179`.
- Keep all data local.
